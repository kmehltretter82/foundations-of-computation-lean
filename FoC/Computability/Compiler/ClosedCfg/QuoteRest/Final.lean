import FoC.Computability.Compiler.ClosedCfg.QuoteRest.Output

set_option doc.verso true

/-!
# Source-rest finisher endpoint

The source-rest finisher is exposed through
{name (full := FoC.Computability.EncRewriters.BoundedLayoutRunner.SelectedProjectionInputQuoterFiniteLeaf.AssemblySourceRestFinishEquivConstruction)}`AssemblySourceRestFinishEquivConstruction`;
normalized output follows from
{name (full := FoC.Computability.EncRewriters.BoundedLayoutRunner.SelectedProjectionInputQuoterFiniteLeaf.AssemblySourceRestFinishEquivConstruction.toOutputConstruction)}`AssemblySourceRestFinishEquivConstruction.toOutputConstruction`.
The former exact-tape construction was removed because the live-tail joiner
provably cannot shrink its 109-cell context to the advertised 107-cell target.
-/
